<!-- Main Content -->

{{#each forums}}
<a href="show/{{this.postID}}" class="forumwhite"> {{this.postBy}}
    <h2 class="post-title card-header header">{{this.postSubject}}<a href="delete/{{this.postID}}"><i class="forumwhite fas fa-trash-alt"
                id="{{this.postID}}"></i>
        </a> </h2>
    <div class="card-body mb-2">

        <div class="post-subtitle nav-item" scope="col">
            <a class="nav-link">
                {{this.postBody}}
            </a>
        </div>

        <ul>
            {{#each comment}}
            <li class="nav-item">
                <a class="nav-link">
                    {{this.postedBy}}
                </a>
                <h7> {{this.postBody}}</h7>
            </li>
        </ul>
        {{/each}}
        <input class="form-control mt-5 post-body" id="post-body"></input>
        <button type="button" class="btn btn-secondary submitPost mt-1" id="{{this.postID}}">Submit</button>
    </div>
</a>

{{/each}}
<!-- Pager -->
<div class="clearfix">
    <a class="btn btn-primary float-right mt-3" href="#">Older Posts &rarr;</a>
</div>
</div>
</div>
</div>

<hr>